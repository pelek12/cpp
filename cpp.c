#include <stdio.h>
#include "cpopolsku.h"


liczba_calkowita pryncypalny(pustka) {

    dla (liczba_calkowita i rowny zero; i mniejszy_niz 10; i jeden_w_gore) {
        jezeliby tak_wypadlo_ze i mniejszy_niz 5 to_wtedy kontynuuj;
        wydrukuj(na_ekranie_liczbe i);
    }

    gdyby_tak_sie_zdarzylo_przypadkiem_ze(nie_masz_racji) zwroc wskazanie_ze_masz_problem;

    zwroc wskazanie_ze_wszystko_sie_powiodlo;
}
